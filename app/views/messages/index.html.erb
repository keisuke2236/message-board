<h1>メッセージ一覧</h1>
<% @messages.each do |message| %>
    名前：<%= message.name %>
    内容：<%= message.body %>
<% end %>
<br />


<%# form_forには do |f|〜ENDまでの内容が全て送信される %>
<%= form_for(@message) do |f| %>
名前：
<%= f.text_field :name %>
内容
<%= f.text_field :body %>
<%= f.submit %>
<% end %>

